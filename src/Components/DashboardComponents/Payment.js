import { Typography } from '@mui/material';
import React from 'react';

const Payment = () => {
     return (
          <div>
               <Typography variant="h4" sx={{ fontFamily: 'Macondo, cursive', fontWeight: 'bold', textAlign: 'center' }}>Online Payment Option is Coming Soon</Typography>
          </div>
     );
};

export default Payment;